services:
  db:
    image: mysql:latest
    restart: always
    env_file: ".env"
    ports:
      - "13306:3306"
    volumes:
      - ./data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./db/my.cnf:/etc/mysql/my.cnf
